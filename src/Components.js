export var pepe = 'pepe'
var ema = 'ema'

export default ema

// lo que se exporte normalmente, como pepe, debe ser importado con el mismo nombre y entre llaves
// solo una cosa puede ser exportada como default, y no importa el nombre al ser importada
